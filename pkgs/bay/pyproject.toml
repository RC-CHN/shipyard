[project]
name = "bay"
version = "0.1.0"
description = "Bay API service for Agent Sandbox"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlmodel",
    "pydantic",
    "pydantic-settings",
    "aiohttp",
    "docker",
    "aiodocker",
    "podman>=5.0.0",
    "aiosqlite",
    "alembic",
    "greenlet>=3.2.4",
    "python-multipart>=0.0.20",
    "tomli>=2.0.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "requests>=2.31.0",
]
